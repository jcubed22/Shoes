<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <title>Kelly's Shoes: Let's Get Em!</title>
    </head>
    <body>
        <div class="container">
            <h1>Available Brands</h1>
            <hr>
            {% if brands is not empty %}
                <p>Choose a brand to find which stores carry it.</p>
                <ul>
                    {% for brand in brands %}
                        <li><a href="/brands/{{ brand.getId }}">{{ brand.getName }}</a></li>
                    {% endfor %}
                </ul>
            {% else %}
                <p>No brands appear to have been added.  Fill out the form below to add one.</p>
            {% endif %}

            <form action="/brands" method="post">
                <div class="form-group">
                    <label for="name">Enter the name of the brand:</label>
                    <input class="form-control" id="name" placeholder="Brand name" name="name" type="text" required>
                </div>
                <button class="btn btn-success" type="submit">Add brand</button>
            </form>

            <form action="/brand/{{ brand.getId }}" method="post">
                <input name="_method" type="hidden" value="delete">
                <button class="btn btn-danger" type="submit" onclick="return confirm('Are you sure you want to delete this brand?')">Delete brand</button>
            </form>

            <p><a href="/">Home</a></p>
        </div>
    </body>
</html>
